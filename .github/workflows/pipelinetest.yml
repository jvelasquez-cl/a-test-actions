name: pipelinetest
on: push

env:
  USERNAME: ${{ github.event.push.user.login }}

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: user-step
        run: echo "${{ github.event.push.user.login }}"
